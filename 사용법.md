# 매일 이렇게만 하면 됨 (5분 안에 읽기)

## 처음 한 번만 (설치)

1. GitHub 계정 만들고, 초대 메일 수락 (retyper가 collaborator 초대 보냄)
2. 터미널(PowerShell) 열고 한 줄씩 입력:
   ```
   git clone https://github.com/retyper/GREplanner.git
   cd GREplanner
   claude
   ```
   - `git` 또는 `claude`가 없다고 나오면: [Git 설치](https://git-scm.com/download/win), [Claude Code 설치](https://claude.com/claude-code) 후 다시
3. Claude가 켜지면 이렇게 입력: **"나는 학습자 B(정치·통계)야. 기억해줘"** → 끝

## 매일 아침 (공부 시작)

1. 터미널 열기 → `cd GREplanner` → `claude`
2. **`/gre-daily`** 입력
3. Claude가 오늘 날짜에 맞는 할 일을 알려줌 → 그대로 따라가면 됨

## 공부하는 중에

전부 그냥 말로 하면 됨. 예시:

| 하고 싶은 것 | 이렇게 입력 |
|---|---|
| 오늘 뭐 해야 하는지 다시 보기 | "오늘 할 일 뭐였지?" |
| 개념 모르겠을 때 | "표준편차 비교 문제 푸는 법 알려줘" |
| 문제 더 풀고 싶을 때 | "RC 지문 하나 내줘" |
| 문제 틀렸을 때 | "이 문제 틀렸어: (문제 내용)" → Claude가 오답노트에 기록해줌 |
| 단어 퀴즈 | **`/vocab-quiz`** |
| 에세이 쓴 뒤 채점 | **`/awa-grade`** 하고 에세이 붙여넣기 |

## 매일 밤 (공부 끝)

1. **`/gre-sync`** 입력 → 오늘 기록이 GitHub에 자동 업로드됨 (상대방 기록도 받아옴)
2. 끝. 창 닫아도 됨

## 이것만 기억

- 시작: `/gre-daily` · 끝: `/gre-sync` — **이 두 개만 치면 나머지는 Claude가 다 함**
- 모르면 그냥 한국어로 물어보면 됨 (Claude가 이 폴더의 계획표를 다 알고 있음)
- 첫날(8/14)은 특별히: ETS 계정 만들고 PowerPrep Test 1 모의고사부터 보라고 Claude가 안내해줄 것임
